# RECURRENT EXPERIENCE REPLAY IN DISTRIBUTED REINFORCEMENT LEARNING 

 ICLR 2019, from google

论文提出了一种架构叫做Recurrent Replay Distributed DQN (缩写为R2D2)，在Atari的57款游戏中，有52款超过了人类的表现。bison在Ms. Pacman任务中亲测比普通DQN性能明显更强（6000分 vs 3000分），但也没有能够通第一关。

## 1. Ms. Pacman 的实验结果

论文中 R2D2 在 Ms. Pacman 上报告的分数达到数万分；作为对照，你目前的普通 DQN 实验约为 3000 分，尚不能进入下一个关卡：

- R2D2：42,281.7
- Ape-X：11,255.2
- IMPALA：约 6,501～7,342
- 人类基准：6,951.6

这里的 42,281.7 是 3 个训练随机种子的最终分数平均值，采用随机 no-op 开局设置（最多 30 个 no-op，并非指只评估 30 局）；每局最多运行 30 分钟，即 108,000 个 Atari frames，报告的是未折扣的 episode return。因此，它不是某一局偶然打出的最高分，也不是只训练几百万帧就能达到的结果。论文明确说明 R2D2 报告最终分数，而 Ape-X 使用的是训练期间最高分。来源：[R2D2.pdf](R2D2.pdf)，第 14–17 页及 Table 4。

## 2. R2D2 的关键设计

R2D2 可以理解为：

> Ape-X 式分布式优先经验回放 + Recurrent DQN/LSTM + 正确的序列训练方法

它不是简单地把普通 DQN 的全连接层替换成 LSTM。

### 2.1 网络结构

R2D2 使用：

```text
4-frame stack
    ↓
DQN 的三层卷积网络
    ↓
512 单元 LSTM
    ↓
Dueling Q-network
    ├── Value head
    └── Advantage head
```

此外，LSTM 的输入不仅包括图像特征，还包括上一步的：

- reward
- one-hot action

这样 LSTM 可以利用“我刚才做了什么、得到了什么反馈”来建立轨迹状态。

### 2.2 不再存单步 transition，而是存序列

普通 DQN 通常存：

```text
(s_t, a_t, r_t, s_{t+1})
```

R2D2 存的是长度为 80 的连续片段：

```text
(s_t, a_t, r_t),
(s_{t+1}, a_{t+1}, r_{t+1}),
...
(s_{t+79}, a_{t+79}, r_{t+79})
```

相邻序列重叠 40 步，并且不跨越 episode 边界。

这是训练 LSTM 的关键，因为 LSTM 必须看到连续轨迹，而不是随机打乱的单帧。

### 2.3 Stored state + burn-in

这是 R2D2 最重要的设计之一。

每条序列还保存 actor 生成经验时的起始 recurrent state。对于 LSTM，它包含隐藏状态 h 和细胞状态 c，详见第 5 节。按照论文第 4 页与附录 Table 2，训练时：

```text
前缀 40 步：burn-in，用于调整 LSTM 状态
后续 80 步：学习段，计算有效位置的 TD loss
```

也就是说，论文中的 80 步学习序列另外附带 burn-in 前缀：

```text
[前缀 40 步 burn-in] [后续 80 步学习段]
```

为什么需要这样做？

如果每次从零 hidden state 开始训练，序列开头的 Q 值通常很不准确，BPTT 会把这些错误梯度传播到 LSTM，造成所谓的 destructive updates。R2D2 用两种方式缓解：

- stored state：使用采样时保存的历史 hidden state；
- burn-in：先让当前网络读入一段历史，恢复出较合理的 hidden state，再开始计算 loss。

论文实验显示，两者结合效果最好。对于需要长期行为记忆的任务，这一点尤其重要。即使 Ms. Pacman 看起来基本可观测，LSTM 也能帮助学习更好的时序表示和策略。

### 2.4 5-step Double Q-learning

R2D2 使用 5-step return，并采用 Double DQN 的动作选择方式。暂时省略价值缩放和终止处理：

```text
a* = argmax_a Q_online(s_{t+5}, a)
target = r_t
       + γ r_{t+1}
       + ...
       + γ^5 Q_target(s_{t+5}, a*)
```

论文设置：

```text
n = 5
γ = 0.997
target network 每 2500 次 learner update 更新一次
```

相比普通 DQN 常见的 γ=0.99，0.997 更重视较远期的回报。这对 Ms. Pacman 这种“连续吃豆、吃水果、清屏、保命”组成的长期收益可能有帮助。

### 2.5 不裁剪 reward，而是 value rescaling

普通 DQN 常把 reward 裁剪成：

```text
-1, 0, +1
```

R2D2 不直接裁剪 reward，而是使用可逆的 value function rescaling：

```text
h(x) = sign(x) (sqrt(|x| + 1) - 1) + εx
```

其中 ε=0.001。

这样做的目的，是压缩极大的 Q 值范围，同时保留不同奖励大小之间的信息。Ms. Pacman 中吃水果、吃鬼、过关等奖励差异较大，完全 reward clipping 会丢掉这些尺度信息。

### 2.6 序列级 prioritized replay

R2D2 不是只按单个 transition 的 TD error 进行优先级排序，而是给整条序列分配优先级：

```text
p = 0.9 × max(|δ_i|)
  + 0.1 × mean(|δ_i|)
```

长序列中如果只使用 mean TD error，大错误可能被平均掉。因此 R2D2 强调 max TD error，让包含关键事件的片段更容易被重新训练。

另外使用：

```text
priority exponent = 0.9
importance-sampling exponent = 0.6
```

### 2.7 分布式 actor-learner

R2D2 训练时大约使用：

```text
256 个并行 actor
1 个 GPU learner
batch size = 64
每批是 64 条长度 80 的学习序列，另带 burn-in 前缀
```

actor 负责持续产生经验，learner 从共享 prioritized replay 中采样训练。论文训练规模达到数十亿甚至 100 亿级别的 environment frames。

这点非常重要：R2D2 的 42k 分数不能和“单个环境、训练几千万帧的普通 DQN”直接比较。它既有算法改进，也有更大的数据吞吐和训练规模。

## 3. 如何理解普通 DQN 的约 3000 分

这其实并不意外。普通 DQN 和论文中的 R2D2 存在多个差异：

| 方面 | 普通 DQN | R2D2 |
|---|---|---|
| 经验单位 | 单步 transition | 长序列 |
| 网络 | CNN | CNN + 512 LSTM |
| replay | transition prioritized 或 uniform | sequence prioritized |
| recurrent state | 无 | stored state + burn-in |
| return | 常见 1-step | 5-step Double Q |
| 折扣率 | 常见 0.99 | 0.997 |
| reward | 通常 clipping | value rescaling |
| 数据采集 | 单 actor | 约 256 actors |
| 训练规模 | 通常较小 | 数十亿/百亿 frames |
| 策略表示 | 主要依赖当前帧 | 使用完整行为历史 |

因此，3,000 分并不表示实现失败；它大约已经接近普通 DQN 在这个游戏上的典型水平。42,000 分更像是一个完整的大规模 R2D2 系统的结果，而不是单个局部技巧带来的提升。

## 4. Ms. Pacman 的改进与对照实验建议

如果你目前只有一个普通 DQN 环境，不建议一开始就复制 256 个 actor。比较实际的升级顺序是：

1. 先加入 Double DQN、Dueling network 和 n-step return。

2. 从单步 replay 改成序列 replay，例如：

```text
burn-in length = 40
learning length = 80
prefix + learning length = 120
# 另需足够的数据构造末尾的 5-step target，或屏蔽无有效目标的位置
```

3. 使用 CNN + LSTM，并保存每条序列起始时的完整状态 (h, c)。

4. 使用 5-step target，尝试：

```text
gamma = 0.997
```

5. 保留原始 reward，加入 value rescaling；同时做 reward clipping 对照实验。

6. 对序列而不是单步 transition 做 prioritized replay，优先级使用 max TD error 与 mean TD error 的混合。

7. 增大训练数据量和 actor 数量。即使只能做到 8～32 个 actor，也通常比单 actor 更接近 R2D2 的训练条件。

8. 确认评估设置一致：随机 no-op 开局（最多 30 个 no-op）、多局平均、不要只看训练过程中的偶然最高分。

一个简化版的训练目标可以写成：

```text
(Q_online_t, z_online_{t+1}) = online(obs_t, action_{t-1}, reward_{t-1}, z_online_t)
# z = (h, c)；online 和 target 各自沿历史递推

a* = argmax_a Q_online(s_{t+5}, a)

y_t = H(
      r_t + γr_{t+1} + ... + γ^4r_{t+4}
      + γ^5 H^{-1}(Q_target(s_{t+5}, a*))
      )

loss = Huber(Q_online(s_t, a_t) - stop_gradient(y_t))
```

其中 H 表示价值缩放函数，避免与 LSTM 的 h 混淆。这里用 Huber 展示实现示意，并非论文逐行代码；loss 只在 burn-in 后的有效时间步上计算，burn-in 梯度边界见下文。

最关键的结论是：对你这个问题，优先级最高的不是把普通 DQN 的 CNN 再加深，而是改成“连续序列 replay + LSTM + burn-in + stored recurrent state”。论文的消融实验显示，去掉 LSTM 后 Ms. Pacman 曲线明显变差；论文作者还指出，LSTM 在 Atari 中不仅提供记忆，也改善了表示学习。

## 5. LSTM、Stored State 与 Burn-in 的计算细节

本节依据论文第 2.3、3、5 节及附录，展开前向传播和反向传播。论文没有提供逐行自动微分代码；涉及 `no_grad()`、`detach()` 和缓存布局的内容会作为实现说明，而非原文代码。

核心理解：回放的是一段历史，训练的是沿这段历史运行的 Q 网络。Stored state 提供历史记忆的起点，burn-in 用当前网络调整这个起点，后面的学习段承担梯度更新。

### 5.1 h_t 与 c_t：LSTM 维护两份状态

普通 RNN 通常只有隐藏状态 h_t，而 R2D2 使用的 LSTM 维护两份状态：

- **c_t：cell state（细胞状态）**，是内部记忆，通过遗忘门和输入门决定保留什么、写入什么。
- **h_t：hidden state（隐藏状态）**，是当前对外输出的状态，由内部记忆经过变换和输出门得到，供后续 Q 输出头使用。

按常见 LSTM 下标约定，核心关系为：

$$
c_t=f_t\odot c_{t-1}+i_t\odot\tilde c_t,\qquad
h_t=o_t^{\mathrm{gate}}\odot\tanh(c_t).
$$

其中 f_t、i_t、o_t^{gate} 分别是遗忘门、输入门、输出门，候选内容为 tilde c_t，乘号表示逐元素乘法。这些门和候选内容由当前输入与上一步 h_{t-1} 计算。这里给输出门加 gate 标记，以免与后文的环境观测 o_t 混淆。

可以把 c_t 理解为内部记事本，h_t 是这一刻从记事本中拿出来给后续网络看的内容。两者通常维度相同，但数值和作用不同；把它们严格称为“长期记忆”和“短期记忆”并不准确。

**保存、恢复或截断 LSTM 状态，都要同时处理 (h_t, c_t)。**只恢复 h_t、把 c_t 清零，无法恢复原来的 LSTM 记忆。论文有时用一个 h_t 统称整个 recurrent state，应与这里展开后的两个分量区分。

### 5.2 网络参数、循环状态与三种网络角色

以下统一用 z_t=(h_t,c_t) 表示完整状态，并将 z_t 定义为处理当前观测之前的状态；这与上一小节常见 LSTM 公式的输出下标约定相差一步：

$$
(Q_t,z_{t+1})=F_\theta(o_t,a_{t-1},r_{t-1},z_t).
$$

前向传播改变状态 z_t，优化器改变参数 theta。不计算梯度时，LSTM 状态照样会变化。

| 角色 | 参数与状态 | 用途 |
|---|---|---|
| Actor：与环境交互 | 较旧的参数副本、持续递推的状态 | 产生动作、奖励、观测和 stored state；不做反向传播 |
| Online：learner 当前网络 | 当前参数 theta、由回放历史重算的状态 | 计算待拟合 Q 值，也为 Double DQN 选择 bootstrap 动作 |
| Target：learner 目标网络 | 参数 theta^-、独立递推的状态 | 评估 bootstrap 动作的价值；不接受梯度更新 |

“action DQN”如果指训练中的主网络，就是 online；实际与环境交互的 actor 则是另一份可能滞后的副本。论文每 400 个环境步同步 actor 参数，每 2500 次 learner 更新将 online 参数复制给 target。

### 5.3 Stored state：保存记忆数值，不保存计算图

假设 actor 已经玩到第 1000 步，我们抽到从这里开始的片段。若把 LSTM 清零，网络会丢掉此前积累的记忆。游戏中途的状态通常不等于 episode 开始时的零状态。

Stored state 将 actor 当时的完整状态 z_1000 保存下来，训练时用它初始化片段。它必须与片段起点对齐；保存的是状态数值，不是环境状态、模型参数或 actor 的计算图。

这个状态由旧参数生成，而当前网络可能已经改变了内部表示，因此旧状态与当前参数不完全匹配。这就是 representational drift / recurrent state staleness。它可能比零状态好，却不是当前网络从 episode 开始重新运行得到的精确状态。

训练时只用 stored state 初始化，随后连续递推。不要每一步都用 actor 保存的状态覆盖当前状态，否则会切断序列中的递归计算关系。论文 Figure 1 使用逐步 stored states 比较 Q 值，是诊断误差的过程，不能直接当作训练流程。

### 5.4 Burn-in 的前向时间轴与梯度边界

按第 4 页公式和附录 Table 2，配置是 40 步 burn-in 前缀加 80 步学习段。暂时省略 5-step 目标需要的尾部数据：

```text
相对时间        0 ........ 39       40 ................ 119
输入            burn-in 前缀        学习段
online   stored z --前向递推--> z_online_40 --前向递推，计算 Q-->
                                  |
                             detach 边界
                                  |
梯度            不回传到此前       <---学习段内 BPTT----------

target   stored z --前向递推--> z_target_40 --前向递推，提供目标-->
                 整条 target 分支均不进行反向传播
```

两条链读取同样的历史输入，但使用不同参数，所以 burn-in 后的状态通常不同。Target 必须运行自己的 CNN、LSTM 和 Q 头；不能拿 online 的 LSTM 输出，只换成 target 输出头。图中两条链以同一个 actor stored state 作数值初始化，是常见实现方式；论文没有逐行规定这部分缓存代码。

Burn-in 是每次抽取回放片段后的前向计算，不是训练开始前做一次预热。它可能缓解初始状态失配，但不能保证完全消除失配，也不能凭空恢复前缀中不存在的历史信息。

实现上通常对 burn-in 使用 `no_grad()`，并在进入学习段时对 h、c 都 `detach()`。原文表述是只展开前缀，在剩余部分更新网络；这里说明的是明确截断 burn-in 梯度的实现方式。**仅仅不计算前缀 loss，并不自动阻止后续 loss 通过递归连接回传进前缀。**

### 5.5 Double DQN：当前预测、未来选动作与未来估值

先不考虑价值缩放和终止事件。对于学习段某个时刻 t，R2D2 使用 n=5：

$$
a^*_{t+5}=\arg\max_a Q^\theta_{t+5}(a),\qquad
y_t=\sum_{k=0}^{4}\gamma^k r_{t+k}+\gamma^5 Q^-_{t+5}(a^*_{t+5}).
$$

以平方误差示意梯度关系：

$$
L_t=\tfrac12\left[Q^\theta_t(a_t)-\operatorname{stopgrad}(y_t)\right]^2.
$$

- **当前预测 Q_online_t(a_t)**：用 online，取 replay 中当年真正执行的动作 a_t，而不是当前网络重新选的动作。
- **未来动作 a*_{t+5}**：用当前 online 在未来时刻的输出做 argmax，用于构造目标，不通过这条选动作分支反向传播。
- **未来价值 Q_target_{t+5}(a*_{t+5})**：用 target 对刚才选中的同一个动作打分，整个 y_t 当作常数。

例如未来 online 输出 `[10, 8]`，target 输出 `[6, 9]`：online 选第一个动作，bootstrap 取 target 的 **6**，不是 9。取 9 就变成让 target 自己选动作，不是这里的 Double DQN。

为了得到时刻 t+5 的两个输出，两张网络都沿 replay 中真实发生的动作、奖励、观测递推到那里。不能把当前 online 重新选出的动作塞回历史，也不需要模拟接下来五步新轨迹。5-step 使用已发生的五个奖励，再加一个未来价值估计。

### 5.6 价值缩放如何进入目标

用大写 H 表示论文的价值缩放函数，以免与 LSTM 隐藏状态 h 混淆：

$$
H(x)=\operatorname{sign}(x)(\sqrt{|x|+1}-1)+10^{-3}x.
$$

实际目标为：

$$
y_t=H\!\left(\sum_{k=0}^{4}\gamma^k r_{t+k}
+\gamma^5H^{-1}\!\left(Q^-_{t+5}(a^*_{t+5})\right)\right).
$$

网络输出的是变换后的价值：先对 bootstrap 反变换，加原始奖励，再变换回来。不能直接把原始奖励加到变换后的 Q 值上。整个目标依然停止梯度；由于 H 单调递增，使用变换后的 Q 值做 argmax 不改变动作排序。

### 5.7 BPTT 究竟回传多远

以学习段从 40 开始、考察 L_45 为例：梯度从当前预测 Q_online_45(a_45) 进入 Q 头、LSTM，并沿递归链回到 44、43……40，更新这些计算共享的参数以及相关 CNN 参数；到 burn-in 结束的状态处停止。它不会沿目标 y_45 回传到时刻 50 的 target 或 online 选动作分支。

```text
L_45 -> Q_online_45 -> LSTM_45 -> LSTM_44 -> ... -> LSTM_40 -> 停止
  |
  +-- y_45 为常数：不沿目标分支回传到时刻 50
```

5-step return 与 BPTT 长度是两件事：前者决定目标包含多少步奖励，后者决定当前预测的梯度能沿历史展开多远。学习段有多个 L_t，通常汇总成批次 loss 后统一 `backward()` 和 `optimizer.step()`；不要在相邻时间步之间更新参数，也不要每一步 detach，否则就失去了跨步梯度。

Stored state 可以携带学习窗口之前的信息，前向记忆也可以持续超过 80 步；但本次更新不会沿保存的状态回传到当年生成它的计算。记忆跨度可以超过直接梯度跨度，却不意味着任意长程依赖都容易学会。

### 5.8 一次 learner 更新及边界处理

1. 从 prioritized replay 抽取一批连续片段，取得起始 stored state、历史动作、奖励、观测和有效位置标记。
2. Online 与 target 分别从起始状态展开 burn-in；online 在学习段入口截断 h、c 的梯度。
3. Online 对学习段建立计算图；target 在无梯度模式下展开。两者都需要计算有效 bootstrap 位置的 Q 值。
4. 对各有效学习位置，用实际动作取 online 当前预测，用未来 online 选动作、未来 target 估值，构造停止梯度的 5-step 目标。
5. 汇总学习段 loss，结合优先采样的重要性权重，更新 online；根据序列 TD error 更新回放优先级。Burn-in 和 padding 位置不作为学习损失位置。
6. 达到同步周期时，将 online 参数复制给 target。下一批重新从其自己的起始状态展开，不能沿用上一批无关片段的最终状态。

若 80 个学习位置都要构造完整 5-step 目标，必须提供足够的尾部奖励和 bootstrap 观测，否则应屏蔽无有效目标的位置，不能越界补零假装有效。论文没有逐行规定尾部缓存布局，这属于实现时必须补齐的边界处理。

真正的 episode 终止发生在五步以内时，只累加到终止的奖励，移除后续 bootstrap，并在新 episode 开始时重置完整 LSTM 状态。时间上限截断需要按照所用环境的终止语义单独处理，不能不加区分地与真正终止混用。

标准 R2D2 不把 Atari 掉一条命当作 episode 终止，因此不会仅因掉命重置 LSTM 或截断价值 bootstrap。论文的 replay 序列不跨 episode；若具体实现使用 padding，必须屏蔽无效位置。
